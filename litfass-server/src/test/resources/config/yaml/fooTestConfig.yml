collection: foo
flow:
  - script:
      description: "First step"
      language: kts
      code: println("foo")
  - http:
      description: "Second step"
      url: https://some.url/foo?bar=true
      username: user
      password: secret
  - script:
      language: kts
      code: println("bar")
